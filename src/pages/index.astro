---
import Layout from '../layouts/Layout.astro';
import LeadForm from '../components/LeadForm.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('posts')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 2);

const features = [
  {
    title: "Jämför mäklare",
    description: "Få offerter från kvalitetssäkrade mäklare i ditt område.",
    icon: "📊"
  },
  {
    title: "Helt kostnadsfritt",
    description: "Vår tjänst är helt gratis för dig som säljare.",
    icon: "💰"
  },
  {
    title: "Tryggt val",
    description: "Vi hjälper dig hitta rätt mäklare för din bostad.",
    icon: "🛡️"
  }
];
---

<Layout title="Jämför mäklare gratis">


	<main>
		<!-- Hero Section -->
		<section class="relative bg-neutral pt-20 pb-24 lg:pt-32 lg:pb-40 overflow-hidden">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="flex flex-col lg:flex-row items-center gap-20">
					<!-- Left Content -->
					<div class="lg:w-3/5 text-left">
						<div class="inline-block bg-white px-4 py-1.5 rounded-full border border-slate-200 text-xs font-bold tracking-widest text-[#0071e3] uppercase mb-8">
							#1 TJÄNSTEN FÖR BOSTADSFÖRSÄLJNING
						</div>
						
						<h1 class="text-5xl md:text-7xl font-bold text-slate-900 mb-8 leading-[1.05] tracking-tight">
							Jämför erbjudanden <br />
							<span class="text-secondary">från lokala mäklare</span>
						</h1>
						
						<p class="text-xl text-slate-600 mb-12 max-w-2xl leading-relaxed">
							Ska du sälja lägenhet eller hus? Vi matchar dig med de bäst lämpade mäklarna i ditt område. 
							Jämför arvoden och resultat på bara några minuter – helt kostnadsfritt.
						</p>

						<div class="grid grid-cols-1 sm:grid-cols-2 gap-6 mb-12">
							{[
								"Hitta bästa mäklaren för dig",
								"Få högre försäljningspris",
								"Spara tid och pengar",
								"Kvalitetssäkrade mäklare"
							].map((benefit) => (
								<div class="flex items-center space-x-3 group">
									<div class="flex-shrink-0 w-6 h-6 rounded-full bg-white border border-slate-200 flex items-center justify-center group-hover:bg-secondary group-hover:border-secondary transition-colors">
										<svg class="h-4 w-4 text-secondary group-hover:text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
											<path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" />
										</svg>
									</div>
									<span class="text-slate-800 font-medium">{benefit}</span>
								</div>
							))}
						</div>

						<div class="flex items-center space-x-4">
							<div class="flex -space-x-3">
								{[1, 2, 3, 4].map(i => (
									<div class="w-10 h-10 rounded-full border-2 border-white bg-slate-200 overflow-hidden">
										<img src={`https://i.pravatar.cc/100?img=${i+10}`} alt="User" />
									</div>
								))}
							</div>
							<div class="text-sm">
								<div class="flex text-yellow-500 mb-1">
									{[1, 2, 3, 4, 5].map(() => (
										<svg class="h-3 w-3 fill-current" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/></svg>
									))}
								</div>
								<p class="text-slate-500">Över 10,000 nöjda säljare</p>
							</div>
						</div>
					</div>

					<!-- Right Content (Form) -->
					<div class="lg:w-2/5 w-full">
						<div class="bg-white rounded-4xl premium-shadow p-6 md:p-10 border border-slate-100">
							<div class="mb-8 text-center">
								<h3 class="text-2xl font-bold mb-2">Hitta din mäklare idag</h3>
								<p class="text-slate-500">Det tar mindre än 2 minuter</p>
							</div>
							<LeadForm />
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Steps Section -->
		<section class="py-32 bg-white">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="text-center mb-24">
					<h2 class="text-4xl md:text-5xl font-bold mb-6 tracking-tight">
						Tre enkla steg till en <br />
						<span class="text-slate-400">bättre försäljning</span>
					</h2>
					<p class="text-xl text-slate-500 max-w-2xl mx-auto">
						Vi gör det svåra valet enkelt genom en kvalitetssäkrad matchningsprocess.
					</p>
				</div>

				<div class="grid grid-cols-1 md:grid-cols-3 gap-12">
					{[
						{
							title: "Berätta om din bostad",
							text: "Svara på korta frågor om ditt behov och din bostads specifika egenskaper.",
							img: "/images/step-1.jpg"
						},
						{
							title: "Bli matchad",
							text: "Vår intelligenta tjänst letar upp de mäklare som presterar bäst i ditt kvarter.",
							img: "/images/step-2.jpg"
						},
						{
							title: "Välj och sälj",
							text: "Jämför offerter i lugn och ro. Du förbinder dig aldrig till någonting.",
							img: "/images/step-3.jpg"
						}
					].map((step, i) => (
						<div class="flex flex-col group">
							<div class="relative mb-10 overflow-hidden rounded-4xl premium-shadow border border-slate-100">
								<img src={step.img} alt={step.title} class="w-full h-full object-cover grayscale-[0.2] transition-all duration-700 group-hover:grayscale-0 group-hover:scale-110" />
								<div class="absolute top-6 left-6 w-12 h-12 bg-white/90 backdrop-blur rounded-2xl flex items-center justify-center text-xl font-bold text-slate-900 border border-white/50">
									{i + 1}
								</div>
							</div>
							<h3 class="text-2xl font-bold mb-4 px-2">{step.title}</h3>
							<p class="text-lg text-slate-600 leading-relaxed px-2">{step.text}</p>
						</div>
					))}
				</div>
			</div>
		</section>


		<!-- Maximize Odds Section -->
		<section class="py-32 bg-neutral">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="text-center mb-20">
					<h2 class="text-4xl md:text-5xl font-bold mb-6 tracking-tight">
						Maximera oddsen för en <br />
						<span class="text-secondary">lyckad affär</span>
					</h2>
					<p class="text-xl text-slate-500 max-w-2xl mx-auto">
						Vi fokuserar på de faktorer som faktiskt gör skillnad för ditt slutpris.
					</p>
				</div>

				<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
					{[
						{ title: "Säljstrategi", text: "Hur tänker mäklaren gå till väga för att nå rätt köpare?", icon: "🎯" },
						{ title: "Erfarenhet", text: "Vilka lyckade försäljningar i närområdet har mäklaren genomfört?", icon: "📈" },
						{ title: "Mäklararvode", text: "Vilken kostnadsstruktur gäller och vad ingår i tjänsten?", icon: "💰" },
						{ title: "Personkemi", text: "Fungerar kontakt och tillit? En viktig grund för samarbetet.", icon: "🤝" }
					].map((item) => (
						<div class="premium-card p-10 flex flex-col">
							<div class="text-4xl mb-6">{item.icon}</div>
							<h3 class="text-xl font-bold mb-4">{item.title}</h3>
							<p class="text-slate-600 leading-relaxed">{item.text}</p>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- Why Compare Section -->
		<section class="py-32 bg-white">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="grid grid-cols-1 lg:grid-cols-2 gap-24 items-center">
					<div class="relative">
						<div class="relative rounded-4xl overflow-hidden premium-shadow aspect-[4/3]">
							<img src="/images/slider-woman.png" alt="Mäklare" class="w-full h-full object-cover" />
						</div>
						<!-- Floating Stat -->
						<div class="absolute -bottom-10 -right-10 bg-white p-8 rounded-3xl premium-shadow border border-slate-100 hidden md:block">
							<div class="text-4xl font-bold text-[#0071e3] mb-1">24%</div>
							<p class="text-slate-500 font-medium whitespace-nowrap">Genomsnittlig prisökning</p>
						</div>
					</div>

					<div class="text-left">
						<div class="inline-block bg-neutral px-4 py-1.5 rounded-full text-xs font-bold tracking-widest text-slate-400 uppercase mb-8">
							VARFÖR JÄMFÖRA?
						</div>
						<h2 class="text-4xl md:text-5xl font-bold text-slate-900 mb-8 leading-tight tracking-tight">
							Det lönar sig att jämföra – varje gång
						</h2>
						<p class="text-xl text-slate-600 mb-10 leading-relaxed">
							Att jämföra mäklare ger insikt i vad som faktiskt skiljer dem åt – i pris, strategi och engagemang. 
							När man ser alternativen bredvid varandra blir det tydligt var styrkorna finns.
						</p>
						
						<ul class="space-y-6">
							{[
								"Hitta mäklaren med bäst lokalkännedom",
								"Säkerställ en marknadsmässig arvodesnivå",
								"Få tillgång till dolda köpekontakter",
								"Trygghet genom hela processen"
							].map(point => (
								<li class="flex items-start">
									<svg class="h-6 w-6 text-secondary mt-1 mr-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
									</svg>
									<span class="text-lg text-slate-800 font-medium">{point}</span>
								</li>
							))}
						</ul>
					</div>
				</div>
			</div>
		</section>


		<!-- News & Tips Section -->
		<section class="py-32 bg-neutral">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="flex flex-col md:flex-row justify-between items-end mb-16 gap-8">
					<div class="max-w-2xl">
						<div class="inline-block bg-white px-4 py-1.5 rounded-full text-xs font-bold tracking-widest text-slate-400 uppercase mb-6">
							INSIKTER & GUIDER
						</div>
						<h2 class="text-4xl md:text-5xl font-bold tracking-tight">Nyheter, guider & säljtips</h2>
					</div>
					<a href="/blog" class="text-[#0071e3] font-bold text-lg hover:underline flex items-center">
						Visa alla nyheter
						<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
							<path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" />
						</svg>
					</a>
				</div>

				<div class="grid grid-cols-1 md:grid-cols-2 gap-12">
					{posts.map((post) => (
						<article class="premium-card flex flex-col group overflow-hidden">
							<div class="relative h-80 overflow-hidden">
								<img src={post.data.heroImage} alt={post.data.title} class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
								<div class="absolute top-6 left-6 bg-white/90 backdrop-blur px-4 py-1.5 rounded-full text-xs font-bold text-slate-900 border border-white/50">
									{post.data.category || 'GUIDE'}
								</div>
							</div>
							<div class="p-10 flex flex-col flex-grow">
								<h3 class="text-2xl font-bold mb-6 group-hover:text-[#0071e3] transition-colors leading-tight">
									<a href={`/blog/${post.slug}`}>{post.data.title}</a>
								</h3>
								<div class="mt-auto flex items-center justify-between">
									<span class="text-slate-400 font-medium">Läs mer</span>
									<div class="w-12 h-12 rounded-full border border-slate-200 flex items-center justify-center group-hover:bg-slate-900 group-hover:text-white transition-all">
										<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
											<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
										</svg>
									</div>
								</div>
							</div>
						</article>
					))}
				</div>
			</div>
		</section>

		<!-- FAQ Section -->
		<section class="py-32 bg-white">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="grid grid-cols-1 lg:grid-cols-2 gap-24">
					<div>
						<div class="inline-block bg-neutral px-4 py-1.5 rounded-full text-xs font-bold tracking-widest text-slate-400 uppercase mb-6">
							HJÄLP & SUPPORT
						</div>
						<h2 class="text-4xl md:text-5xl font-bold mb-8 tracking-tight">Vanliga frågor</h2>
						<p class="text-xl text-slate-500 mb-10 leading-relaxed">
							Här hittar du svar på de vanligaste frågorna om att sälja bostad och hur vår matchningstjänst fungerar.
						</p>
						<a href="/faq" class="inline-flex items-center px-8 py-4 bg-slate-900 text-white font-bold rounded-full hover:bg-slate-800 transition-colors shadow-xl">
							Se alla frågor
						</a>
					</div>

					<div class="space-y-4">
						{[
							{ q: "Hur fungerar OffertHem?", a: "Vi matchar dig med de bäst lämpade mäklarna i ditt område baserat på din bostads unika förutsättningar. Du fyller i formuläret, och inom kort får du svar från mäklare som vill sälja din bostad." },
							{ q: "Kostar det något att använda tjänsten?", a: "Nej, tjänsten är helt gratis för dig som säljare. Vi får istället en ersättning från mäklarna som är anslutna till vår tjänst." },
							{ q: "Hur många mäklare får jag offerter från?", a: "Vanligtvis blir du kontaktad av upp till 3-4 mäklare som har erfarenhet av just ditt område och din typ av bostad." },
							{ q: "Hur vet jag att mäklarna är seriösa?", a: "Alla mäklare i vårt nätverk är registrerade hos Fastighetsmäklarinspektionen (FMI) och har genomgått vår kvalitetssäkring." }
						].map((item, i) => (
							<div class="border-b border-slate-100 pb-4">
								<button class="faq-button w-full flex items-center justify-between text-left group py-6">
									<span class="text-xl font-bold text-slate-900 group-hover:text-[#0071e3] transition-colors">{item.q}</span>
									<div class="w-10 h-10 rounded-full bg-neutral flex items-center justify-center text-slate-400 group-hover:bg-secondary group-hover:text-white transition-all faq-icon">
										<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
											<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
										</svg>
									</div>
								</button>
								<div class="faq-answer hidden mt-2 mb-8 text-lg text-slate-500 leading-relaxed max-w-2xl">
									{item.a}
								</div>
							</div>
						))}
					</div>
				</div>
			</div>
		</section>

		<!-- Trust & CTA Section -->
		<section class="py-32 bg-white mb-20">
			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
				<div class="relative rounded-4xl overflow-hidden min-h-[600px] flex items-center justify-center premium-shadow">
					<img src="/images/trust-meeting.png" alt="Möte" class="absolute inset-0 w-full h-full object-cover grayscale-[0.2]" />
					<div class="absolute inset-0 bg-slate-900/40"></div>
					
					<div class="relative z-10 glass p-12 md:p-20 rounded-4xl max-w-3xl text-center">
						<h2 class="text-4xl md:text-5xl font-bold text-white mb-8 tracking-tight">Kvalitet i varje steg</h2>
						<p class="text-xl text-white/90 mb-12 leading-relaxed">
							Ta det första steget mot en bostadsaffär du kan känna dig helt säker på. 
							Vi hjälper dig att jämföra erfarna, noggrant utvalda mäklare – kostnadsfritt och utan förpliktelser.
						</p>
						<div class="flex flex-col sm:flex-row gap-6 justify-center">
							<a href="/#form" class="bg-white text-slate-900 px-10 py-5 rounded-full font-bold text-lg hover:bg-neutral transition-all shadow-2xl">
								Kom igång nu
							</a>
							<a href="/om-oss" class="glass px-10 py-5 rounded-full font-bold text-lg text-white hover:bg-white/20 transition-all">
								Läs mer om oss
							</a>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Popula Cities Section -->
        <section class="py-24 bg-neutral">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
				<div class="inline-block bg-white px-4 py-1.5 rounded-full text-xs font-bold tracking-widest text-slate-400 uppercase mb-12">
					LOKALA EXPERTER
				</div>
                <h2 class="text-3xl font-bold mb-16">Populära områden</h2>
                <div class="grid grid-cols-2 md:grid-cols-4 gap-12">
					{[
						{ name: "Stockholm", link: "/jamfor-maklare/stockholm" },
						{ name: "Göteborg", link: "/jamfor-maklare/goteborg" },
						{ name: "Malmö", link: "/jamfor-maklare/malmo" },
						{ name: "Uppsala", link: "/jamfor-maklare/uppsala" }
					].map(city => (
						<a href={city.link} class="text-lg font-semibold text-slate-900 hover:text-[#0071e3] transition-colors">{city.name}</a>
					))}
                </div>
            </div>
        </section>

	</main>

	<script>
		const faqButtons = document.querySelectorAll('.faq-button');
		
		faqButtons.forEach(button => {
			button.addEventListener('click', () => {
				const answer = button.nextElementSibling;
				const icon = button.querySelector('.faq-icon');
				
				// Toggle active class on button
				button.classList.toggle('active');
				
				// Toggle hidden class on answer
				answer.classList.toggle('hidden');
				
				// Rotate icon
				if (icon) {
					icon.style.transform = button.classList.contains('active') ? 'rotate(45deg)' : 'rotate(0deg)';
				}
			});
		});
	</script>

</Layout>
